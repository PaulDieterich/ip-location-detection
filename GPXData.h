#include <iostream>
#include <string.h>
using namespace std;
//https://stackoverflow.com/questions/1120140/how-can-i-read-and-parse-csv-files-in-c
//class to represent IP2LOCATOPN-LITE-DB-5 CSV 
/*class GPXData{
    private: 
        unsigned long ip;
        unsigned long startIP;
        unsigned long endIP;
        string country; 
        string region; 
        string city; 
        unsigned long latitude; 
        unsigned long longitude; 

    public: 
        GPXData(unsigned long _ip, unsigned long _startIP, unsigned long _endIP, string _country, string _region, string _city, unsigned long _latitude, unsigned long _longitude): ip(_ip), startIP(_startIP), endIP(_endIP), country(_country), region(_region), city(_city), latitude(_latitude), longitude(_longitude){}




};*/
